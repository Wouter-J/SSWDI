@model AS_Core.DomainModel.Lodging

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Lodging</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID" />
            <div class="form-group">
                <label asp-for="LodgingType" class="control-label"></label>
                <select asp-for="LodgingType" asp-items="Html.GetEnumSelectList<AS_Core.Enums.LodgingType>()" class="form-control">
                </select>
                <span asp-validation-for="LodgingType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MaxCapacity" class="control-label"></label>
                <input asp-for="MaxCapacity" class="form-control" />
                <span asp-validation-for="MaxCapacity" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AnimalType" class="control-label"></label>
                <select asp-for="AnimalType" asp-items="Html.GetEnumSelectList<AS_Core.Enums.AnimalType>()" class="form-control">
                </select>
                <span asp-validation-for="AnimalType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
